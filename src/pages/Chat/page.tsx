
type Props = { processoid: string; };

export default function ChatPage({ processoid }: Props) {

    return (
        <div className="w-full h-full flex justify-center items-center">
            <h1>Chat Page - Em construção</h1>
        </div>
    );
}